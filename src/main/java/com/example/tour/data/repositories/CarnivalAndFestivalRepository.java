package com.example.tour.data.repositories;

import com.example.tour.data.models.CarnivalAndFestival;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarnivalAndFestivalRepository extends JpaRepository<CarnivalAndFestival, Long> {
}
